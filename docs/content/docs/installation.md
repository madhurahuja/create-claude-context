# Installation

This library works flawlessly over `npx` so no permanent installation is strictly required.

To run it immediately:
```bash
npx create-claude-context
```

If you prefer to install it globally for frequent usage:
```bash
npm install -g create-claude-context
```

And then invoke it anywhere:
```bash
create-claude-context
```
